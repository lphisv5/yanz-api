{
  "name": "vercel-api-key-system",
  "version": "1.0.0",
  "description": "Serverless API Key System (Gen / Auth / Check-in) for Vercel",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev"
  },
  "dependencies": {
    "mongodb": "^6.10.0"
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
