{
  "name": "yanz-api-key-system",
  "version": "1.0.0",
  "description": "Vercel Serverless API Key System (Gen key / Auth / Checkin)",
  "type": "module",
  "scripts": {
    "start": "vercel dev"
  },
  "dependencies": {
    "mongodb": "^6.10.0"
  },
  "devDependencies": {
    "vercel": "^28.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
