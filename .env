# .env.example
# Set these in Vercel Environment Variables (Project Settings -> Environment Variables)
PRIMARY_BASE=https://apis.roblox.com
FALLBACK_BASE=https://voxlis.example/api

# Cache TTL in seconds (in-memory; ephemeral on serverless)
CACHE_TTL=300

# Number of retries for HTTP requests
RETRY_COUNT=2
